using CommunityToolkit.Mvvm.ComponentModel;

namespace RemoteFlow.Presentation.ViewModels;

/// <summary>
/// 使用指南页（内嵌）。纯静态内容，无状态、无数据加载；
/// 作为 <see cref="PageTabViewModel.Page"/> 的模板选择键存在。
/// </summary>
public sealed class HelpPageViewModel : ObservableObject
{
}
